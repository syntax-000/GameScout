# GS-002 provider fixtures

GameScout keeps minimized synthetic Steam Store-category fixtures in `tests/fixtures/providers/steam-catalog-fixtures.json` and activity fixtures in `tests/fixtures/providers/steam-fixtures.json`.

Explicit connection-specific categories may create canonical capabilities, but generic categories stay ambiguous and every supported player count remains unknown. Current-player activity cannot create or change compatibility evidence.
